import {bithomeServeSPA} from '../../utils/project/bithome.server';

/**
 * Executes the build process, serving the files of the development environment using an `express` server.
 */
export = bithomeServeSPA;
